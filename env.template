# ===========================================
# AstroBot Environment Configuration Template
# ===========================================
# Copy this file to .env


-Discord Bot Configuration: 
envtoken=YOUR_DISCORD_BOT_TOKEN_HERE

- API Endpoint: 

# Default: http://localhost:8000 (runs Django API locally)
# For production, set this to your deployed API URL
API_BASE_URL=http://localhost:8000

- Django Configuration:
# If not set, Django will use the default insecure key, so this is optional
# Generate a new one: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-secret-key-here


-Postgres Configuration:
# Currently not set, need to host online or locally
# Override these deprecated databse values
DB_NAME=dcp140fdl1dd2c
DB_USER=rhuhdredobaluh
DB_PASSWORD=your_database_password_here
DB_HOST=ec2-34-238-201-192.compute-1.amazonaws.com
DB_PORT=5432

